const sqlCreds = {
  connectionLimit : 10,
  host            : 'localhost',
  user            : 'root', // root for mac users
  password        : 'root', // root for mac users
  database        : 'roku_users',
  port            : 8889 // 8889 for mac users
}

module.exports = sqlCreds;